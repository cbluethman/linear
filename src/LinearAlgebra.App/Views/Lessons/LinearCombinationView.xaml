<UserControl x:Class="LinearAlgebra.App.Views.Lessons.LinearCombinationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <Border DockPanel.Dock="Right" Width="250" Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="12">
                    <TextBlock Text="Basis Vectors" Style="{StaticResource SectionHeader}" />

                    <TextBlock Text="v1" Foreground="#50A0FF" FontWeight="Bold" FontSize="13" Margin="0,4,0,2" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="X:" Foreground="{StaticResource TextSecondaryBrush}" Width="20" VerticalAlignment="Center" />
                        <Slider Value="{Binding V1X}" Minimum="-5" Maximum="5" TickFrequency="0.5" IsSnapToTickEnabled="True" Width="150" />
                        <TextBlock Text="{Binding V1X, StringFormat='{}{0:F1}'}" Foreground="{StaticResource TextBrush}" FontFamily="Consolas" Width="40" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Y:" Foreground="{StaticResource TextSecondaryBrush}" Width="20" VerticalAlignment="Center" />
                        <Slider Value="{Binding V1Y}" Minimum="-5" Maximum="5" TickFrequency="0.5" IsSnapToTickEnabled="True" Width="150" />
                        <TextBlock Text="{Binding V1Y, StringFormat='{}{0:F1}'}" Foreground="{StaticResource TextBrush}" FontFamily="Consolas" Width="40" />
                    </StackPanel>

                    <TextBlock Text="v2" Foreground="#64DC64" FontWeight="Bold" FontSize="13" Margin="0,8,0,2" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="X:" Foreground="{StaticResource TextSecondaryBrush}" Width="20" VerticalAlignment="Center" />
                        <Slider Value="{Binding V2X}" Minimum="-5" Maximum="5" TickFrequency="0.5" IsSnapToTickEnabled="True" Width="150" />
                        <TextBlock Text="{Binding V2X, StringFormat='{}{0:F1}'}" Foreground="{StaticResource TextBrush}" FontFamily="Consolas" Width="40" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Y:" Foreground="{StaticResource TextSecondaryBrush}" Width="20" VerticalAlignment="Center" />
                        <Slider Value="{Binding V2Y}" Minimum="-5" Maximum="5" TickFrequency="0.5" IsSnapToTickEnabled="True" Width="150" />
                        <TextBlock Text="{Binding V2Y, StringFormat='{}{0:F1}'}" Foreground="{StaticResource TextBrush}" FontFamily="Consolas" Width="40" />
                    </StackPanel>

                    <Separator Background="{StaticResource BorderBrush}" Margin="0,12" />

                    <TextBlock Text="Scalars" Style="{StaticResource SectionHeader}" />
                    <TextBlock Text="s1 (for v1)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                    <Slider Value="{Binding Scalar1}" Minimum="-3" Maximum="3" TickFrequency="0.25" IsSnapToTickEnabled="True" />
                    <TextBlock Text="{Binding Scalar1, StringFormat='{}{0:F2}'}" Foreground="{StaticResource TextBrush}" FontFamily="Consolas" HorizontalAlignment="Center" />

                    <TextBlock Text="s2 (for v2)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,8,0,0" />
                    <Slider Value="{Binding Scalar2}" Minimum="-3" Maximum="3" TickFrequency="0.25" IsSnapToTickEnabled="True" />
                    <TextBlock Text="{Binding Scalar2, StringFormat='{}{0:F2}'}" Foreground="{StaticResource TextBrush}" FontFamily="Consolas" HorizontalAlignment="Center" />
                </StackPanel>
            </ScrollViewer>
        </Border>
        <Border />
    </DockPanel>
</UserControl>
