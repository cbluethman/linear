<UserControl x:Class="LinearAlgebra.App.Views.ExplanationPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:LinearAlgebra.App.Converters">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolVis" />
    </UserControl.Resources>

    <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
        <Grid Margin="16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding ExplanationText}" Foreground="{StaticResource TextBrush}"
                           FontFamily="Consolas" FontSize="13" TextWrapping="Wrap" />

                <!-- Quiz feedback -->
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0"
                            Visibility="{Binding IsQuizMode, Converter={StaticResource BoolVis}}">
                    <TextBlock Text="{Binding QuizPrompt}" Foreground="{StaticResource AccentBrush}"
                               FontWeight="SemiBold" FontSize="13" Margin="0,0,16,0" />
                    <TextBlock Text="{Binding QuizFeedback}" FontSize="13" FontWeight="Bold">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding QuizFeedback}" Value="Correct!">
                                        <Setter Property="Foreground" Value="#50C878" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding QuizFeedback}" Value="Try again!">
                                        <Setter Property="Foreground" Value="#FF5050" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Text="{Binding QuizCorrect, StringFormat=' Score: {0}/'}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="16,0,0,0" />
                    <TextBlock Text="{Binding QuizTotal}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                </StackPanel>
            </StackPanel>

            <Button Grid.Column="1" Content="{Binding IsQuizMode, Converter={x:Static converters:QuizButtonTextConverter.Instance}}"
                    Command="{Binding ToggleQuizCommand}" Style="{StaticResource PresetButton}"
                    VerticalAlignment="Top" MinWidth="100" />
        </Grid>
    </Border>
</UserControl>
